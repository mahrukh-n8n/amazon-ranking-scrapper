---
phase: 04-scheduling-automation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/ui/main_window.py
  - src/scraper_controller.py
autonomous: true
must_haves:
  truths:
    - "Closing the window minimizes the app to the system tray instead of exiting (if enabled)."
    - "User can restore the app or trigger a 'Run Now' from the system tray menu."
    - "Scheduling and webhook settings are persisted across app restarts."
  artifacts:
    - path: "settings.json"
      provides: "Persistent storage for app settings"
  key_links:
    - from: "src/ui/main_window.py"
      to: "settings.json"
      via: "json.dump/load"
---

<objective>
Implement system tray integration for background execution and persist application settings to disk.
</objective>

<execution_context>
@C:\Users\Glorvax\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Glorvax\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/ui/main_window.py
@src/scraper_controller.py
@.planning/phases/04-scheduling-automation/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Implement Settings Persistence</name>
  <files>src/ui/main_window.py</files>
  <action>
    - Implement `save_settings()` and `load_settings()` methods in `MainWindow`.
    - Save to `settings.json` in the root directory.
    - Persist: Webhook URL, Webhook Enabled, Notifications Enabled, Schedule Enabled, Recurrence, Time, Minimize to Tray.
    - Call `load_settings()` in `__init__`.
    - Call `save_settings()` whenever a setting changes.
  </action>
  <verify>Change a setting, restart the app, and verify the setting is preserved.</verify>
  <done>Settings are persisted to disk.</done>
</task>

<task type="auto">
  <name>Implement System Tray Integration</name>
  <files>src/ui/main_window.py</files>
  <action>
    - Add `QSystemTrayIcon` to `MainWindow`.
    - Create a context menu for the tray icon with:
      - "Restore": Shows the main window.
      - "Run Job Now": Triggers `on_start_clicked`.
      - "Exit": Truly exits the application.
    - Add a "Minimize to Tray on Close" checkbox in the Settings group.
    - Override `closeEvent(self, event)`:
      - If "Minimize to Tray" is enabled, hide the window and `event.ignore()`.
      - Otherwise, `event.accept()`.
    - Show tray notifications when a scheduled job starts or finishes (if minimized).
  </action>
  <verify>Minimize the app and verify it stays in the tray. Close the app and verify it minimizes if setting is checked.</verify>
  <done>App can run in the background via the system tray.</done>
</task>

</tasks>

<verification>
1. Enable "Minimize to Tray on Close".
2. Close the window; verify the app is still in the system tray.
3. Right-click tray -> Restore; verify the window reappears.
4. Restart the app; verify settings like Webhook URL are still there.
</verification>

<success_criteria>
App maintains state and can run background scheduled jobs without a visible window.
</success_criteria>

<output>
After completion, create .planning/phases/04-scheduling-automation/04-02-SUMMARY.md
</output>
