---
phase: 04-scheduling-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/scraper_controller.py
  - src/ui/main_window.py
autonomous: true
must_haves:
  truths:
    - "User can enable scheduling and set a specific daily/weekly time."
    - "The app displays the next scheduled run time in the UI."
    - "A job starts automatically at the scheduled time if the app is open."
  artifacts:
    - path: "src/scraper_controller.py"
      provides: "AsyncIOScheduler integration"
    - path: "src/ui/main_window.py"
      provides: "Scheduling configuration UI"
  key_links:
    - from: "src/ui/main_window.py"
      to: "src/scraper_controller.py"
      via: "schedule_job call"
---

<objective>
Implement core scheduling logic using apscheduler and add the UI controls to MainWindow.
</objective>

<execution_context>
@C:\Users\Glorvax\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Glorvax\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/scraper_controller.py
@src/ui/main_window.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Add apscheduler dependency</name>
  <files>pyproject.toml</files>
  <action>
    Add `apscheduler = "^3.10.4"` to the dependencies in `pyproject.toml`.
    Run `poetry install` (or let the environment handle it).
  </action>
  <verify>Check `pyproject.toml` for the new dependency.</verify>
  <done>Dependency is added.</done>
</task>

<task type="auto">
  <name>Implement Scheduling in Controller</name>
  <files>src/scraper_controller.py</files>
  <action>
    - Import `AsyncIOScheduler` from `apscheduler.schedulers.asyncio`.
    - Initialize `self.scheduler = AsyncIOScheduler()` in `__init__`.
    - Start the scheduler in `__init__`.
    - Add a `job_started` signal to `ScraperController`.
    - Implement `schedule_job(recurrence: str, time_str: str)` method.
      - It should clear existing scheduled jobs for scraping.
      - It should add a new job to the scheduler that calls `self.start_job()`.
      - Recurrence: 'Daily' (cron: hour, minute) or 'Weekly' (cron: day_of_week, hour, minute).
    - Implement `get_next_run()` which returns the timestamp of the next scheduled run.
    - Update `start_job` to emit `job_started` signal.
  </action>
  <verify>Verify scheduler is initialized and `schedule_job` correctly adds a task.</verify>
  <done>Controller can manage a scheduled scraping job.</done>
</task>

<task type="auto">
  <name>Add Scheduling UI to MainWindow</name>
  <files>src/ui/main_window.py</files>
  <action>
    - Add a new `QGroupBox("Scheduling")` to the main layout.
    - Inside, add:
      - `cb_schedule_enabled`: QCheckBox("Enable Scheduled Runs")
      - `combo_recurrence`: QComboBox (Daily, Weekly)
      - `time_edit`: QTimeEdit
      - `lbl_next_run`: QLabel("Next Run: None")
    - Connect `cb_schedule_enabled.toggled`, `combo_recurrence.currentTextChanged`, and `time_edit.timeChanged` to a method `update_scheduling()`.
    - `update_scheduling()` should call `self.controller.schedule_job(...)` if enabled, otherwise cancel.
    - Update `lbl_next_run` whenever scheduling is updated.
    - Connect `self.controller.job_started` to a method that updates UI buttons (Start/Load disabled, Stop/Pause enabled) similar to `on_start_clicked`.
  </action>
  <verify>Run the UI, check if the Scheduling section is visible and interacts with the controller.</verify>
  <done>User can configure and see scheduled runs in the UI.</done>
</task>

</tasks>

<verification>
1. Schedule a run for 1 minute from now.
2. Verify "Next Run" shows the correct time.
3. Verify the job starts automatically when the time is reached.
</verification>

<success_criteria>
User can schedule jobs from the UI and they execute automatically.
</success_criteria>

<output>
After completion, create .planning/phases/04-scheduling-automation/04-01-SUMMARY.md
</output>
